import { Vector, Matrix } from "../src";

describe("trans-vector2d", () => {
  it("has Vector", () => {
    expect(Vector).not.toBeUndefined();
  });

  it("has Matrix", () => {
    expect(Matrix).not.toBeUndefined();
  });
});
